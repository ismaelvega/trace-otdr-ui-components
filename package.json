{
  "name": "trace-otdr-ui-components",
  "private": true,
  "workspaces": [
    "packages/ui",
    "apps/demo"
  ],
  "scripts": {
    "dev": "npm --workspace apps/demo run dev",
    "build": "npm --prefix packages/ui run build && npm --prefix apps/demo run build",
    "test": "npm --prefix packages/ui run test",
    "lint": "npm --prefix packages/ui run lint",
    "typecheck": "npm --prefix packages/ui run typecheck"
  },
  "devDependencies": {
    "jsdom": "^28.1.0"
  }
}
